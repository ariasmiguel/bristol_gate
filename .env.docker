# Bristol Gate Docker Environment Variables
# This file is used inside Docker containers

# Required API Keys (set these via docker-compose environment or external .env)
FRED_API_KEY=${FRED_API_KEY}
EIA_TOKEN=${EIA_TOKEN}

# Container-specific settings
DB_PATH=/app/data/bristol_gate.duckdb
DOWNLOAD_DIR=/app/downloads

# Chrome/Selenium settings for containerized web scraping
CHROME_DRIVER_PATH=/usr/bin/chromedriver
CHROME_BIN=/usr/bin/chromium

# Logging
LOG_LEVEL=INFO

# Performance settings for containers
PYTHONUNBUFFERED=1
PYTHONDONTWRITEBYTECODE=1

# Disable Chrome sandbox for container environment
CHROME_NO_SANDBOX=true 